import { css } from '@emotion/core';

const styles = css`
  .social-links {
    margin-top: 15px;
    font-size: 24px;

    li:not(:first-of-type) {
      margin-left: 10px;
    }
  }

  .share-link {
    font-size: 30px;
  }
`;

export default styles;
